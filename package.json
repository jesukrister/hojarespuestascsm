{
  "name": "lector-hojas-respuesta",
  "version": "1.0.0",
  "private": true,
  "description": "Genera hojas de respuesta, las lee desde una foto y corrige pruebas de selección múltiple en el navegador.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "start": "npx --yes serve ."
  },
  "engines": {
    "node": ">=20"
  }
}
